<template>
    <div class="form-wrapper">
        <h1>Signing in...</h1>
    </div>
</template>
<script>

export default {
    created() {
        /* const decodedToken = jwt_decode(this.$auth.$storage.getUniversal('_token.auth0'))
            console.log(decodedToken)
            this.$auth.$storage.setUniversal('jwt_decode', decodedToken) */
    }
}
</script>